#include <iostream>
#include <fstream>
using namespace std;

//TODO: NOT implemented
int main(int argc, char** argv) {
    ifstream infile("datatower.txt");
    int data[10][10];
    int count = 0, temp;
    while (!infile.eof()) {
        infile>>temp;
        count++;
        if (count )
        cout<<row<<" ";
    }
    
    int data[5][5];

    



}
